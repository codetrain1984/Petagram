import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#135e04',
      light: 'cyan',
    },
    secondary: {
      main: '#9c058a',
    },
    otherColor: {
      main: '#006BDF',
    },
  },
})
